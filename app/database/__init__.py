"""Main Database objects in other places of the app.

engine - for DB connectivity, 
SessionLocal(Factory of Session obiects)
Base - base class for models or classes(ORM models) to inherit

Additionally DBInitializer service for operations connected 
with Database initialization after project startup.
"""
